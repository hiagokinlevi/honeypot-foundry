# SSH observation server settings
SSH_LISTEN_PORT=2222
SSH_HOST_KEY_PATH=./hostkey
# HTTP observation server settings
HTTP_LISTEN_PORT=8080
# Event output (stdout and/or file)
EVENT_OUTPUT_FILE=events.jsonl
# Maximum events to buffer before flush
EVENT_BUFFER_SIZE=100
